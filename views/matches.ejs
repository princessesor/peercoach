<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Potential Matches</title>
  <link rel="stylesheet" href="/styles/matches.css">
  <!-- <link rel="stylesheet" href="/styles/main.css"> -->
</head>
<body>
  <h1>Suggested Peer Matches for <%= user.name %></h1>
  <ul> </ul>
  <% if (matches.length > 0) { %>
    <ul>
      <% matches.forEach(user => { %>
        <li>
          <h3><%= user.name %></h3>
          <p><strong>Bio:</strong> <%= user.bio %></p>
          <p><strong>Skills Desired:</strong> <%= user.skillsDesired.join(', ') %></p>
          <p><strong>Skills They Have:</strong> <%= user.skillsHave.join(', ') %></p>
          
          <button onclick="window.location.href='/chat/<%= user._id %>'">Chat</button>

        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No matches found.</p>
  <% } %>
  
  <button onclick="window.location.href='/'">Back to Home</button>

</body>
</html>